"use client"

export default function RtFooter() {
  return (
    <footer style={{ padding: "24px", borderTop: "1px dashed var(--t-fg)", fontFamily: "var(--font-terminal)", textAlign: "center" }}>
      <div>EOF. Connection closed by foreign host.</div>
    </footer>
  )
}
